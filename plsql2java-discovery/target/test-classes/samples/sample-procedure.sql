CREATE OR REPLACE PROCEDURE UPDATE_SALARY(p_id IN NUMBER, p_amount IN NUMBER) IS
BEGIN
  UPDATE EMPLOYEES SET SALARY = SALARY + p_amount WHERE EMPLOYEE_ID = p_id;
  COMMIT;
EXCEPTION
  WHEN OTHERS THEN ROLLBACK;
END UPDATE_SALARY;
/
